<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cqing.project00">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- SD卡写权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 使用相机 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.PopularMoviesActivity"
            android:parentActivityName=".activity.MainActivity"
            android:label="@string/popular_movies"
            />
        <activity android:name=".activity.MovieDetailActivity"
            android:parentActivityName=".activity.PopularMoviesActivity"
            android:label="@string/movie_detail"/>
        <activity android:name=".activity.CollectionActivity"
            android:parentActivityName=".activity.PopularMoviesActivity"
            android:label="@string/movie_collection"/>
        <provider
            android:authorities="com.cqing.project00"
            android:name=".data.PopMoviesProvider"
            />
    </application>

</manifest>